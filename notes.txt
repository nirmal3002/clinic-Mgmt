STEPS TO FINISH THE CREATE BOOK OPERATION LOCALLY

add a proxy in the json file
use the absolute URL of the backend instead of just "/api/books"
initiate builtin libraries first, then call the custom made exported pacakges
initiate json, cors, db at the earliest possible

